# [docs:start:deps]
[project]
name = "iris-tutorial"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "edgeflow",
    "mlflow>=3.11.1,<4",
    "numpy>=2.4.4,<3",
    "scikit-learn>=1.8.0,<2",
]
# [docs:end:deps]

[tool.uv]
package = false
