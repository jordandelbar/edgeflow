[project]
name = "edgeflow-example-k3d-yolo"
version = "0.0.0"
description = "Edgeflow tutorial 05: deploy YOLOv8n on k3d with WASM image pre/post."
requires-python = ">=3.12"
dependencies = [
    "edgeflow",
    "mlflow",
    "ultralytics",
]

[tool.uv]
package = false
